[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "my_project"
version = "0.1.0"
dependencies = [
    "requests"
]
optional-dependencies = { 
    test = ["pytest"],
    dev = ["black"]
}